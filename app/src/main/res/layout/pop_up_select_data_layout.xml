<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="15dp"
    android:paddingRight="15dp" >
    <LinearLayout
        android:id="@+id/pop_up_select_data_title"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/pop_up_select_data_title_cancel"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:layout_weight="1"
            android:text="取消"
            android:textSize="14sp"
            android:textColor="@color/gray_666666"/>
        <TextView
            android:id="@+id/pop_up_select_data_title_confirm"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:gravity="center_vertical|right"
            android:layout_weight="1"
            android:text="确定"
            android:textSize="14sp"
            android:textColor="@color/gray_666666"/>
    </LinearLayout>
    <ListView
        android:id="@+id/pop_up_select_data_list_view"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/pop_up_select_data_title"
        android:divider="#f1f1f1"
        android:layout_marginBottom="20dp"
        android:overScrollMode="never"
        android:listSelector="@color/transparent">
    </ListView>
</RelativeLayout>
